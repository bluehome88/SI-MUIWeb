

/*
( function ( $ ) {
    "use strict";
    $( document ).ready( function () {
        var $heading = $( ".fw-settings-form-header h2" );
        $heading.html( theme_info.name )
            .prepend( '<div class="logo"></div>' )
            .append( '<span>' + theme_info.version + '</span>' );

        fwEvents.on( 'fw:options:init', function () {
            var $side_tab = $( '.fw-options-tabs-list' ).slice( 0, 1 );
            $( '.fw-options-tabs-wrapper > .fw-options-tabs-contents, #wpbody-content .metabox-holder.fw-options-tabs-contents' ).css( 'min-height', $side_tab.innerHeight() );
        } );
    } );
} )( jQuery );*/

